CREATE TABLE DIRECCION(
IDDIRECCION NCHAR(7) NOT NULL CONSTRAINT PK_IDDIRECCION PRIMARY KEY,  -- THE DIFERENT BETWEEN NCHAR AND CHAR IS, NCHAR ?à•A1☼ASDFH ACCEPTS SYMBOLS AS CHAR DON´T
DISTRITO NVARCHAR2(50) NOT NULL, --THE DIFERENT BETWEEN VARCHAR AND NVARCHAR IS NVARCHAR JUST USE THE NUMBERS OF CHARACTERS USED AND THE LEFT IT´S DELETED
DIRECCION NVARCHAR2(50) NOT NULL  --NOT NULL MEANS IT CAN NOT BE EMPTY

);

CREATE TABLE PERSONA(
--DNI PRIMARY KEY = PK
DNI CHAR(10),
NOMBRE VARCHAR (50) NOT NULL,
PATERNO VARCHAR (50) NOT NULL,
MATERNO VARCHAR (50) NOT NULL,
FECHA_NACIMIENTO DATE NOT NULL,
SEXO CHAR (1) CONSTRAINT CH_SEXO CHECK (SEXO IN ('M', 'F'))
);